@using DasBlog.Web.Models.BlogViewModels;
@model ListCommentsViewModel

@if (Model != null && Model.Comments != null)
{ 
<div class="post-comments">
    @Html.Partial("_CommentAddPartial", new AddCommentViewModel() { Comment = string.Empty, Email = string.Empty, HomePage = string.Empty, Name = string.Empty, TargetEntryId = Model.PostId})

    <div class="post-comments-list">
        @if (Model.Comments.Count > 0)
        {
            foreach (var comment in Model.Comments)
            {
                @Html.Partial("_CommentPartial", comment)
            }
        }
    </div>
</div>
}